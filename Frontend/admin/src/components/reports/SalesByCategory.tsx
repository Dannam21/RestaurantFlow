import { salesByCategory } from '../../data/mockReportsData'
export function SalesByCategory(){return <section className="report-panel category-sales"><header><h2>VENTAS POR CATEGORÍA</h2><select aria-label="Periodo de categorías"><option>Hoy</option></select></header><div className="category-body"><div className="category-donut"><div><strong>S/ 4,870</strong><span>Total</span></div></div><div>{salesByCategory.map(item=><span key={item.category}><i style={{background:item.color}}/><em>{item.category}<small>S/ {item.amount.toLocaleString('en-US')}</small></em><b>{item.percentage}%</b></span>)}</div></div><button>Ver detalle por categoría →</button></section>}
