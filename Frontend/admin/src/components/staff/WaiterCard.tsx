import { ClipboardList } from 'lucide-react'
import type { StaffMember } from '../../types/staff'
import { StaffStatusBadge } from './StaffStatusBadge'
export function WaiterCard({staff,selected,onSelect}:{staff:StaffMember;selected?:boolean;onSelect?:(staff:StaffMember)=>void}){return <button type="button" className={`staff-card${selected?' selected':''}`} onClick={()=>onSelect?.(staff)} aria-selected={selected}><header><span className="pixel-avatar"><img src={staff.avatar} alt=""/></span><div><strong>{staff.name}</strong><small>{staff.roleLabel}</small></div><StaffStatusBadge status={staff.status}/></header><div className="staff-card-info"><span><small>Mesas asignadas</small><b className="table-chips">{staff.assignedTables.length?staff.assignedTables.map(table=><i key={table}>{table}</i>):<em>Ninguna</em>}</b></span><span><small><ClipboardList size={10}/> Órdenes activas</small><strong>{staff.activeOrders}</strong></span></div><div className="performance-line"><span>Rendimiento <b>{staff.performance}%</b></span><i><b style={{width:`${staff.performance}%`}}/></i></div></button>}
